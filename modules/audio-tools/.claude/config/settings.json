{
  "project": {
    "name": "akai-sampler-tools",
    "version": "1.0.0",
    "type": "cli-utility"
  },
  "features": {
    "file_verification": {
      "enabled": true,
      "mandatory": true,
      "description": "Require verification of all file operations with evidence"
    },
    "no_duplicate_files": {
      "enabled": true,
      "mandatory": true,
      "description": "Never create duplicate files during refactoring"
    },
    "interface_first_design": {
      "enabled": true,
      "description": "Use interface-first design patterns for all TypeScript modules"
    },
    "strict_typescript": {
      "enabled": true,
      "description": "Enforce strict TypeScript mode with all safety flags"
    },
    "cross_platform_testing": {
      "enabled": true,
      "description": "Test on multiple platforms (macOS, Linux, Windows)"
    }
  },
  "quality_gates": {
    "typescript_compilation": {
      "required": true,
      "command": "pnpm build"
    },
    "tests": {
      "required": true,
      "command": "pnpm test",
      "coverage_minimum": 80
    },
    "file_verification": {
      "required": true,
      "description": "All file operations must be verified with bash commands"
    },
    "documentation": {
      "required": true,
      "description": "Documentation must be updated for all changes"
    }
  },
  "performance_targets": {
    "disk_format_detection_ms": 100,
    "sample_conversion_seconds": 5,
    "batch_extraction_seconds_per_disk": 30,
    "memory_usage_mb": 500,
    "binary_execution_overhead_ms": 50
  },
  "development": {
    "monorepo_tool": "pnpm",
    "package_manager": "pnpm",
    "test_framework": "vitest",
    "typescript_version": "latest"
  },
  "external_dependencies": {
    "bundled": [
      "mtools (mcopy)",
      "akaitools"
    ],
    "system": [
      "rsnapshot"
    ]
  }
}
